version: '3'
services:
  multicorn:
    image: multicorn:latest
    container_name: pg
    hostname: pg
    environment:
      - PGMAJOR=13
      - CLOUDSMITH_TOKEN=${CLOUDSMITH_TOKEN}
    volumes:
      - ./:/cloudsmith_fdw
    entrypoint: /cloudsmith_fdw/entrypoint.sh
